# 项目总览与本地项目记录交付

日期：2026-09-12。实现分支：codex/knowledge-navigation。

## 本轮结果

- 保留项目页首屏结构、三卡布局、网格背景及蓝绿点缀，统计区明确为三个项目记录、两个开发实践、一个方案探索。
- 卡片统一展示用途、阶段、实践重点和最近进展。
- 下方改为最近实践记录，移除重复项目展开及大块方法说明。五条阶段记录来自两份项目档案，点击直达对应详情并自动展开；返回列表恢复滚动位置。
- 想与做、滑一叠使用共同详情模板，补充参与方式、AI 协作、能力、选择、过程、证据、反思、下一步。
- 想与做封面复用已存在的首页设计存档并标明历史版本；滑一叠用基于代码的流程图，不虚构运行截图或体验入口。
- 内容 Agent 保持方案探索状态，旧待核实历史仍收纳在折叠区。
- 项目档案与后续填写方式见 `docs/guides/project-records.md`。

## 内容依据

想与做：本仓库 src/features、src/app、src/shared 的实际结构，以及前几轮导航与页面交付记录。

滑一叠：只读核对 `/Users/Zhuanz/Desktop/WePicTool` 代码、`docs/current.md` 与本机编辑/手写相关记录，没有修改该项目。

最后复核时 current.md 增加了 2026-09-12 23:06 定向真实调用结果。网站已同步为：审核一次，生成并下载三张 1080×1080 图片；三字体视觉、双端真机与失败诊断仍待验收，默认服务未切换，生产未发布。本轮未独立重新调用云服务。381 项测试属于小程序状态文档的记录，并非本轮重跑。

## 检查

- npm run check：90 个源文件语法检查通过。
- npm test：99/99 通过，包含新增的记录链接、去重、转义和内容边界检查。
- 桌面浏览器检查项目总览、两份详情、阶段直达展开和返回列表位置；修复移除次按钮后主按钮被旧 last-child 样式覆盖的问题。
- 390px 窄屏检查两份详情与项目列表；全站设备覆盖及全部深色页面验收不在本轮结论内。

未提交、合并或推送；保留工作区原有改动。

## 2026-09-13 细节收口

- 详情模块与章节目录改由同一份配置生成，可选内容留空时同时隐藏模块和入口；没有截图与流程资料时采用纯文字介绍，不显示空白图框。
- 关联内容移除重复标题，详情模块标题图标缩小，手机流程图内边距与图标尺寸更紧凑。
- 仅从项目详情返回时恢复列表位置，从其他栏目进入项目首屏不会继承历史滚动位置。
- 记录定位按元素 ID 匹配，不再把数据 ID 拼入 CSS 选择器。
- 语法检查通过，101/101 测试通过。浏览器验证阶段直达、目录无失效目标、返回位置与重新进入首屏；390px 下详情无横向溢出，关联区只显示一个标题。

# 思考栏目交付

分支：codex/knowledge-navigation。未合并主分支，未推送。

## 页面
- 目录：#/learning?kind=NOTE。展示现有正式记录，提供全部、随笔、产品分析、实践复盘分类与关键词搜索；空分类显示明确空状态，不用草稿填充。
- 阅读：#/notes/:id。统一标题、日期、摘要、正文、结论、相关内容和目录。仅渲染有数据的段落。
- 返回目录保留分类、搜索与列表滚动位置。
- 沿用共享容器、网格背景、配色和导航，栏目不使用人物插画，仅保留少量线性图标。
- 既有设计稿文章的独立入口仍保留；本轮正式目录不展示它们。

## 验证
- npm run check：85 个源文件通过。
- npm test：90 个测试通过。
- 浏览器验证：目录 3 篇、实践复盘筛选 1 篇、搜索空结果、阅读页、目录定位、返回保留分类。
- 已编写窄屏适配样式；本轮未进行完整移动设备实测。

## 后续内容
新增记录使用现有 notes 数据字段；标题、摘要、日期、正文由数据驱动，无需为每篇重复布局。当前分类由 kind 和 category 映射，产品分析分类目前没有正式记录。

## 后续完善
- 搜索覆盖所有已显示正文段落，包括问题、例子和下一步。
- 补充窄屏工具栏、长文本换行及键盘焦点样式。
- 语法检查通过，测试更新至 91 项全部通过；桌面预览已复查。
